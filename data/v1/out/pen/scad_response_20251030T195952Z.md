```openscad
// Background color approximation: #f0f0f0 (light gray) - not rendered in OpenSCAD

// Pen body (main cylinder)
color("black") // #1a1a1a
cylinder(h=4, r=0.15, $fn=32);

// Pen tip (cone)
translate([0, -2.3, 0])
rotate([180, 0, 0])
color("#333333")
cylinder(h=0.6, r1=0.12, r2=0, $fn=32);

// Pen clip (thin box)
translate([0.25, 1.8, 0])
color("#444444")
cube([0.3, 1.2, 0.05], center=true);

// Clicker button (small cylinder)
translate([0, 2.1, 0])
color("#2c2c2c")
cylinder(h=0.2, r=0.2, $fn=32);
```